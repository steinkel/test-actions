name: deploy
on:
  push:
    branches:
      - stage
      - uat
      # - master
jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2

  deployStage:
    name: deploy to stage
    if: github.event.ref == 'refs/heads/stage'
    needs: [build]
    runs-on: ubuntu-20.04
    environment:
      name: stage
      url: 'https://example.com'
    steps:
      - name: deploy
        run: echo I am deploying to ${{ secrets.HOST }}
  deployUat:
    name: deploy to uat
    if: github.event.ref == 'refs/heads/uat'
    needs: [build]
    runs-on: ubuntu-20.04
    environment:
      name: uat
      url: 'https://example.com'
    steps:
      - name: deploy
        run: echo I am deploying!
  deployProd:
    name: deploy to prod
    if: github.event.ref == 'refs/heads/master'
    needs: [build]
    runs-on: ubuntu-20.04
    environment:
      name: prod
      url: 'https://example.com'
    steps:
      - name: deploy
        run: echo I am deploying!

#            - name: remote deploy
#             uses: appleboy/ssh-action@master
#              with:
#                  host: ${{ secrets.HOST }}
#                  username: ${{ secrets.USERNAME }}
#                  key: ${{ secrets.KEY }}
#                  port: ${{ secrets.PORT }}
#                  script: XXX
